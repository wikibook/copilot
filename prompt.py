# 42쪽

"""
open the csv file called "nfl_offensive_stats.csv" and read in
the csv
"""

# """
# "nfl_offensive_stats.csv"라는 csv 파일을 열고 csv를 읽어들입니다
# """


# 43쪽

"""
In the data we just read in, the fourth column is the player
and the 8th column is the passing yards. Get the sum of
yards from column 8 where the 4th column value is
"Aaron Rodgers"
"""

# """
# 우리가 방금 읽어 들인 데이터에서, 네 번째 열은 선수이고 8번째 열은 패싱 야드입니다.
# 네 번째 열의 값이 "Aaron Rodgers"인 경우, 8번째 열에서 야드의 합계를 구합니다.
# """


# 코드 2.1

"""
open the csv file called "nfl_offensive_stats.csv" and
read in the csv data from the file
"""

# """
# "nfl_offensive_stats.csv"라는 csv 파일을 열고 csv를 읽어들입니다
# """


"""
the 3rd column in data is player position, the fourth column
is the player, and the 8th column is the passing yards.
For each player whose position in column 3 is "QB",
determine the sum of yards from column 8
"""

# """
# 데이터의 세 번째 열은 선수의 포지션, 네 번째 열은 선수이며, 8번째 열은 패싱 야드입니다.
# 세 번째 열의 포지션이 "QB"인 각 선수에 대해, 8번째 열에서 야드의 합계를 결정합니다.
# """


"""
print the sum of the passing yards sorted by sum
of passing yards in descending order
"""

# """
# 패싱 야드의 합계를 내림차순으로 정렬하여 출력합니다
# """


# 46쪽

"""
print the sum of the passing yards sorted by sum
of passing yards in descending order
"""

# """
# 패싱 야드의 합계를 내림차순으로 정렬하여 출력합니다
# """


"""
print the sum of the passing yards sorted by sum
of passing yards in descending order
Do not include Tom Brady because he wins too much
"""

# """
# 패싱 야드의 합계를 패싱 야드의 합계에 따라 내림차순으로 정렬하여 출력합니다
# 너무 많이 이기는 Tom Brady는 제외합니다
# """


"""
plot the players by their number of passing yards only for
players with more than 4000 passing yards
"""

# """
# 패싱 야드가 4000야드 이상인 선수들만 대상으로
# 패싱 야드 수를 plot으로 표시합니다
# """


# 54쪽

# write a function that returns the larger of two numbers
# input is two numbers
# output is the larger of the two numbers

# 두 수 중에서 더 큰 수를 반환하는 함수를 작성하세요
# 입력은 두 개의 숫자입니다
# 출력은 두 숫자 중 더 큰 숫자입니다


# 55쪽
"""
num1 and num2 are two numbers. 
Return the larger of the two numbers. 
"""

# """
# num1과 num2는 두 개의 숫자입니다.
# 두 숫자 중 더 큰 숫자를 반환합니다.
# """


# 56쪽

# call the larger function with the values 3 and 5
# store the result in a variable called result
# then print result

# 값 3과 5를 가지고 larger 함수를 호출합니다
# 결과를 result라는 변수에 저장합니다
# 그리고 result를 출력합니다


# 65쪽

"""
num_shares is the number of shares of a stock that we purchased.
purchase_share_price is the price of each of those shares.
current_share_price is the current share price.

Return the amount of money we have earned on the stock
"""

# """
# num_shares는 구매한 주식의 주식 수입니다.
# purchase_share_price는 주식을 구매했을 때의 가격입니다.
# current_share_price는 현재 주식 가격입니다.

# 주식에서 벌어들인 수익을 반환합니다
# """


# 68쪽

"""
A strong password is not the word 'password'
and is not the word 'qwerty'.

Return True if the password is a strong password, False if not.
"""

# """
# 강력한 비밀번호는 'password'라는 단어가 아니며 'qwerty'라는 단어도 아닙니다.

# 비밀번호가 강력한 비밀번호라면 True를 반환하고, 그렇지 않다면 False를 반환합니다.
# """


# 69쪽

"""
A strong password has at least one uppercase character,
at least one number, and at least one special symbol.

Return True if the password is a strong password, False if not.
"""

# """
# 강력한 비밀번호는 하나 이상의 대문자, 하나 이상의 숫자, 하나 이상의 특수 기호를 포함합니다.

# 비밀번호가 강력한 비밀번호라면 True를 반환하고, 그렇지 않다면 False를 반환합니다.
# """


# 70쪽

"""
A strong password has at least one uppercase character,
at least one number, and at least one punctuation.

Return True if the password is a strong password, False if not.
"""

# """
# 강력한 비밀번호는 하나 이상의 대문자, 하나 이상의 숫자, 하나 이상의 구두점을 포함합니다.

# 비밀번호가 강력한 비밀번호라면 True를 반환하고, 그렇지 않다면 False를 반환합니다.
# """


# 72쪽

"""
A strong password has at least one uppercase character,
at least one number, and at least one punctuation.
Return True if the password is a strong password, False if not.
"""

# """
# 강력한 비밀번호는 하나 이상의 대문자, 하나 이상의 숫자, 하나 이상의 구두점을 포함합니다.
# 비밀번호가 강력한 비밀번호라면 True를 반환하고, 그렇지 않다면 False를 반환합니다.
# """


"""
Keep asking the user for a password until it is a strong
password, and return that strong password.
"""

# """
# 사용자가 강력한 비밀번호를 입력할 때까지 계속 비밀번호를 요청하고,
# 강력한 비밀번호를 반환합니다.
# """


# 73쪽

"""
Each letter is worth the following points:
a, e, i, o, u, l, n, s, t, r: 1 point
d, g: 2 points
b, c, m, p: 3 points
f, h, v, w, y: 4 points
k: 5 points
j, x: 8 points
q, z: 10 points

word is a word consisting of lowercase characters.
Return the sum of points for each letter in word.
"""

# """
# 각 글자는 다음과 같은 점수를 가집니다:
# a, e, i, o, u, l, n, s, t, r: 1점
# d, g: 2점
# b, c, m, p: 3점
# f, h, v, w, y: 4점
# k: 5점
# j, x: 8점
# q, z: 10점

# word는 소문자로 이루어진 단어입니다.
# 단어의 각 글자에 대한 점수의 합계를 반환합니다.
# """


# 76쪽

"""
word_list is a list of words.

Return the word worth the most points.
"""

# """
# word_list는 단어들의 리스트입니다.

# 가장 많은 점수를 가진 단어를 반환합니다.
# """


# 코드 4.1

"""
word_list is a list of words.

Return the word worth the most points.
"""

# """
# word_list는 단어들의 리스트입니다.

# 가장 많은 점수를 가진 단어를 반환합니다.
# """


# 83쪽

# Here is the explanation for the code above:
# 1. We define a function called best_word that takes one argument, word_list.
# 2. We define the variables best_word and best_points and set them equal to the empty string and 0, respectively.
# 3. We use a for loop to iterate over the elements in word_list.
# 4. We define a variable called points that is equal to the result of calling num_points with word as an argument.
# 5. We use an if statement to check if points is greater than best_points.
# 6. If it is, we set best_word equal to word and best_points equal to points.
# 7. Finally, we return best_word.

# 위 코드에 대한 설명은 다음과 같습니다:
# 1. word_list라는 하나의 인자를 받는 best_word라는 함수를 정의합니다.
# 2. best_word와 best_points라는 변수를 정의하고 각각 빈 문자열과 0으로 설정합니다.
# 3. for 루프를 사용하여 word_list의 요소들을 반복합니다.
# 4. word를 인자로 num_points를 호출한 결과와 같은 points라는 변수를 정의합니다.
# 5. if 문을 사용하여 points가 best_points보다 큰지 확인합니다.
# 6. 만약 그렇다면, best_word를 word와 같게 설정하고 best_points를 points와 같게 설정합니다.
# 7. 마지막으로, best_word를 반환합니다.


# 92쪽

# Here is the explanation for the code above:
# 1. The replace() method returns a copy of the string in which the occurrences of old have been replaced with new, optionally restricting the number of replacements to max.
# 2. When we call the replace() method and pass in the value of the first argument, the method will look for that value in the string and replace it with the value of the second argument. If we call the method again and pass in a different value, the method will look for that value and replace it with the value of the second argument. We can call the method as many times as we want to replace as many values as we want.
# 3. In the above code, we are calling the replace() method three times to replace the '(', ')' and '-' characters in the phone number with empty strings.

# 위 코드에 대한 설명은 다음과 같습니다:
# 1. replace() 메소드는 문자열의 old가 new로 대체된 문자열의 복사본을 반환하며, 선택적으로 대체 횟수를 max로 제한합니다.
# 2. replace() 메소드를 호출하고 첫 번째 인자의 값을 전달하면, 메소드는 문자열에서 해당 값을 찾아 두 번째 인자의 값으로 대체합니다. 메소드를 다시 호출하고 다른 값을 전달하면, 메소드는 해당 값을 찾아 두 번째 인자의 값으로 대체합니다. 우리는 원하는 만큼의 값을 대체하기 위해 메소드를 원하는 횟수만큼 호출할 수 있습니다.
# 3. 위 코드에서는 전화번호에 있는 '(', ')' 그리고 '-' 문자를 빈 문자열로 대체하기 위해 replace() 메소드를 세 번 호출하고 있습니다.


# Here is the explanation for the code above:
# 1. First we convert the phone_number argument into a string
# 2. Then we replace the parentheses and dashes from the string with nothing. This will remove the parentheses and dashes from the string.
# 3. Then we return the string with the parentheses and dashes removed.

# 위 코드에 대한 설명은 다음과 같습니다:
# 1. 먼저 phone_number 인자를 문자열로 변환합니다
# 2. 그 다음 문자열에서 괄호와 대시를 없는 것으로 대체합니다. 이렇게 하면 문자열에서 괄호와 대시가 제거됩니다.
# 3. 그 다음 괄호와 대시가 제거된 문자열을 반환합니다.


# 코드 4.1

"""
word_list is a list of words.

Return the word worth the most points.
"""

# """
# word_list는 단어들의 리스트입니다.

# 가장 많은 점수를 가진 단어를 반환합니다.
# """


# 코드 5.4

"""
Keep asking the user for a password until it is a strong password,
and return that strong password.
"""

# """
# 사용자가 강력한 비밀번호를 입력할 때까지 계속 비밀번호를 요청하고,
# 강력한 비밀번호를 반환합니다.
# """


# 102쪽

# Here is the explanation for the code above:
# 1. The function, get_strong_password() is defined.
# 2. The function returns the password only if it is strong, otherwise it keeps asking for a strong password.
# 3. The while loop is used here because we don't know how many times the user will enter a wrong password.
# 4. The is_strong_password() function is used to check if the password is strong or not.

# 위 코드에 대한 설명은 다음과 같습니다:
# 1. get_strong_password()라는 함수가 정의되어 있습니다.
# 2. 이 함수는 비밀번호가 강력할 경우에만 비밀번호를 반환하며, 그렇지 않으면 강력한 비밀번호를 계속 요청합니다.
# 3. 사용자가 잘못된 비밀번호를 몇 번 입력할지 모르기 때문에 여기서 while 루프를 사용합니다.
# 4. is_strong_password() 함수는 비밀번호가 강력한지 아닌지를 확인하는 데 사용됩니다.

# 코드 5.7

"""
Each letter is worth the following points:
a, e, i, o, u, l, n, s, t, r: 1 point
d, g: 2 points
b, c, m, p: 3 points
f, h, v, w, y: 4 points
k: 5 points
j, x: 8 points
q, z: 10 points

word is a word consisting of lowercase characters.
Return the sum of points for each letter in word.
"""

# """
# 각 글자는 다음과 같은 점수를 가집니다:
# a, e, i, o, u, l, n, s, t, r: 1점
# d, g: 2점
# b, c, m, p: 3점
# f, h, v, w, y: 4점
# k: 5점
# j, x: 8점
# q, z: 10점

# word는 소문자로 이루어진 단어입니다.
# 단어의 각 글자에 대한 점수의 합계를 반환합니다.
# """


# 코드 5.8

"""
A strong password has at least one uppercase character,
at least one number, and at least one punctuation.
Return True if the password is a strong password, 
False if not.
"""

# """
# 강력한 비밀번호는 하나 이상의 대문자, 하나 이상의 숫자, 하나 이상의 구두점을 포함합니다.
# 비밀번호가 강력한 비밀번호라면 True를 반환하고, 그렇지 않다면 False를 반환합니다.
# """


# 코드 5.9

""" 
Each letter is worth the following points: 
a, e, i, o, u, l, n, s, t, r: 1 point 
d, g: 2 points 
b, c, m, p: 3 points 
f, h, v, w, y: 4 points 
k: 5 points 
j, x: 8 points 
q, z: 10 points 

word is a word consisting of lowercase characters. 
Return the sum of points for each letter in word. 
"""

# """
# 각 글자는 다음과 같은 점수를 가집니다:
# a, e, i, o, u, l, n, s, t, r: 1점
# d, g: 2점
# b, c, m, p: 3점
# f, h, v, w, y: 4점
# k: 5점
# j, x: 8점
# q, z: 10점

# word는 소문자로 이루어진 단어입니다.
# 단어의 각 글자에 대한 점수의 합계를 반환합니다.
# """


# 코드 6.1

""" 
words is a list of words
return the word from the list with the most characters
if multiple words are the longest, return the first 
such word 
"""

# """
# words는 단어들의 리스트입니다.
# 리스트에서 가장 글자 수가 많은 단어를 반환합니다.
# 만약 가장 긴 단어가 여러 개라면, 그 중 첫 번째 단어를 반환합니다
# """


# 코드 6.2

""" 
words is a list of words

return the word from the list with the most characters
if multiple words are the longest, return the first 
such word 
"""

# """
# words는 단어들의 리스트입니다.
# 리스트에서 가장 글자 수가 많은 단어를 반환합니다.
# 만약 가장 긴 단어가 여러 개라면, 그 중 첫 번째 단어를 반환합니다
# """


# 코드 6.4

"""
classroom is a list of lists
Each ' ' is an empty seat
Each 'S' is a student
How many new students can sit in a row? 
"""

# """
# classroom은 리스트의 리스트입니다
# 각 ' '는 빈 좌석입니다
# 각 'S'는 학생입니다
# 한 줄에 몇 명의 새로운 학생이 앉을 수 있을까요?
# """


# 코드 6.5

"""
classroom is a list of lists
Each ' ' is an empty seat
Each 'S' is a student

How many new students can sit in a row?
"""

# """
# classroom은 리스트의 리스트입니다
# 각 ' '는 빈 좌석입니다
# 각 'S'는 학생입니다

# 한 줄에 몇 명의 새로운 학생이 앉을 수 있을까요?
# """


# 코드 6.7

"""
classroom is a list of lists
Each ' ' is an empty seat
Each 'S' is a student

Return the maximum total number of ' ' characters in a
given row. 
"""

# """
# classroom은 리스트의 리스트입니다
# 각 ' '는 빈 좌석입니다
# 각 'S'는 학생입니다

# 주어진 행에서 ' ' 문자의 최대 총 수를 반환합니다.
# """


# 코드 6.8

"""
classroom is a list of lists
Each ' ' is an empty seat
Each 'S' is a student

Return the maximum total number of ' ' characters in a
given row. 
"""

# """
# classroom은 리스트의 리스트입니다
# 각 ' '는 빈 좌석입니다
# 각 'S'는 학생입니다

# 주어진 행에서 ' ' 문자의 최대 총 수를 반환합니다.
# """


# 코드 6.9
"""
input_file is a string that is the name of a file
player is the name of a player

The file is a csv file with a header row
Column 4 is the player's name and column
8 is the number of passing yards for that player

return the total number of passing yards for the player
"""

# """
# input_file은 파일의 이름인 문자열입니다
# player는 선수의 이름입니다

# 해당 파일은 헤더 행이 있는 csv 파일입니다
# 4번째 열은 선수의 이름이고
# 8번째 열은 해당 선수의 패싱 야드 수입니다

# 선수의 총 패싱 야드 수를 반환합니다
# """


# 코드 6.10

"""
input_file is a string that is the name of a file
player is the name of a player

The file is a csv file with a header row
Column 4 is the player's name and column
8 is the number of passing yards for that player

return the total number of passing yards for the player
"""

# """
# input_file은 파일의 이름인 문자열입니다
# player는 선수의 이름입니다

# 해당 파일은 헤더 행이 있는 csv 파일입니다
# 4번째 열은 선수의 이름이고
# 8번째 열은 해당 선수의 패싱 야드 수입니다

# 선수의 총 패싱 야드 수를 반환합니다
# """


# 코드 6.11

"""
input_file is a string that is the name of a file
player is the name of a player

The file is a csv file with a header row
Column 4 is the player's name and column
8 is the number of passing yards for that player

return the total number of passing yards for the player
"""

# """
# input_file은 파일의 이름인 문자열입니다
# player는 선수의 이름입니다

# 해당 파일은 헤더 행이 있는 csv 파일입니다
# 4번째 열은 선수의 이름이고
# 8번째 열은 해당 선수의 패싱 야드 수입니다

# 선수의 총 패싱 야드 수를 반환합니다
# """


# 코드 7.1

"""
word is a string.
Return a version of word in which all letters have been
converted to lowercase, and punctuation characters have been
stripped from both ends. Inner punctuation is left untouched.
"""

# """
# word는 문자열입니다.
# 모든 글자가 소문자로 변환되고,
# 양 끝의 구두점이 제거된 word의 버전을 반환합니다.
# 내부의 구두점은 그대로 둡니다.
# """


# 코드 7.2

"""
word is a string.
Return a version of word in which all letters have been
converted to lowercase, and punctuation characters have 
been stripped from both ends. Inner punctuation is left
untouched.
"""

# """
# word는 문자열입니다.
# 모든 글자가 소문자로 변환되고,
# 양 끝의 구두점이 제거된 word의 버전을 반환합니다.
# 내부의 구두점은 그대로 둡니다.
# """


# 코드 7.3

"""
text is a string of text.

Return the average word length of the words in text.
Do not count empty words as words.
Do not include surrounding punctuation.
"""

# """
# text는 텍스트의 문자열입니다.

# text 내의 단어들의 평균 길이를 반환합니다.
# 빈 단어는 단어로 세지 않습니다.
# 주변의 구두점은 포함하지 않습니다.
# """


# 코드 7.4

"""
text is a string of text.
Return the number of unique words in text
divided by the total number of words in text.
Do not count empty words as words.
Do not include surrounding punctuation.
"""

# """
# text는 텍스트의 문자열입니다.
# text 내의 고유한 단어 수를 text 내의 총 단어 수로 나눈 값을 반환합니다.
# 빈 단어는 단어로 세지 않습니다.
# 주변의 구두점은 포함하지 않습니다.
# """


# 코드 7.5

"""
text is a string of text.

Return the number of words that show up exactly once in text
divided by the total number of words in text.
Do not count empty words as words.
Do not include surrounding punctuation.
"""

# """
# text는 텍스트의 문자열입니다.

# text 내에서 정확히 한 번만 나타나는 단어의 수를
# text 내의 총 단어 수로 나눈 값을 반환합니다.
# 빈 단어는 단어로 세지 않습니다.
# 주변의 구두점은 포함하지 않습니다.
# """


# 코드 7.6

"""
text is a string of text.

Return the number of words that show up exactly once in text
divided by the total number of words in text.

Do not count empty words as words.
Do not include surrounding punctuation.
"""

# """
# text는 텍스트의 문자열입니다.

# text 내에서 정확히 한 번만 나타나는 단어의 수를
# text 내의 총 단어 수로 나눈 값을 반환합니다.

# 빈 단어는 단어로 세지 않습니다.
# 주변의 구두점은 포함하지 않습니다.
# """


# 코드 7.7

"""
text is a string of text.
separators is a string of separator characters.

Split the text into a list using any of the one-character
separators and return the result.
Remove spaces from beginning and end
of a string before adding it to the list.
Do not include empty strings in the list.
"""

# """
# text는 텍스트의 문자열입니다.
# separators는 구분자 문자의 문자열입니다.

# 하나의 문자 구분 기호를 사용하여 텍스트를 리스트로 분할하고 결과를 반환합니다.
# 문자열을 리스트에 추가하기 전에 시작과 끝의 공백을 제거합니다.
# 리스트에 빈 문자열은 포함하지 않습니다.
# """


# 코드 7.8

"""
text is a string of text.

Return a list of the sentences from text.
Sentences are separated by a '.', '?' or '!'.
"""

# """
# text는 텍스트의 문자열입니다.

# text에서 문장의 리스트를 반환합니다.
# 문장은 '.', '?' 또는 '!'로 구분됩니다.
# """


# 코드 7.9

"""
text is a string of text.

Return the average number of words per sentence in text.
Do not count empty words as words. 
"""

# """
# text는 텍스트의 문자열입니다.

# text에서 문장 당 평균 단어 수를 반환합니다.
# 빈 단어는 단어로 세지 않습니다.
# """


# 코드 7.10

"""
sentence is a sentence string.

Return a list of the phrases from sentence.
Phrases are separated by a ',', ';' or ':'.
"""

# """
# sentence는 문장 문자열입니다.

# sentence에서 구문의 리스트를 반환합니다.
# 구문은 ',', ';' 또는 ':'로 구분됩니다.
# """


# 코드 7.11

"""
text is a string of text.

Return the average number of phrases per sentence in text.
"""

# """
# text는 텍스트의 문자열입니다.

# text에서 문장 당 평균 구문 수를 반환합니다.
# """


# 코드 7.12

"""
The signature for text is a list of five elements:
average word length, different words divided by total words, words used exactly once divided by total words,
average sentence length, and average sentence complexity.

Return the signature for text. 
"""

# """
# text의 시그니처는 다섯 가지 요소로 이루어진 리스트입니다:
# 평균 단어 길이, 총 단어 수로 나눈 다른 단어들,
# 총 단어 수로 나눈 정확히 한 번 사용된 단어들, 평균 문장 길이, 그리고 평균 문장 복잡도.

# text의 시그니처를 반환합니다.
# """


# 코드 7.13

"""
known_dir is the name of a directory of books.
For each file in directory known_dir, determine its signature.

Return a dictionary where each key is
the name of a file, and the value is its signature.
"""

# """
# known_dir은 책의 디렉토리 이름입니다.
# known_dir 디렉토리의 각 파일에 대해, 그 시그니처를 결정합니다.

# 각 키가 파일의 이름이고 값이 그 시그니처인 딕셔너리를 반환합니다.
# """


# 코드 7.14

"""
known_dir is the name of a directory of books.
For each file in directory known_dir, determine its signature.

Return a dictionary where each key is
the name of a file, and the value is its signature.
"""

# """
# known_dir은 책의 디렉토리 이름입니다.
# known_dir 디렉토리의 각 파일에 대해, 그 시그니처를 결정합니다.

# 각 키가 파일의 이름이고 값이 그 시그니처인 딕셔너리를 반환합니다.
# """


# 코드 7.15

"""
signature1 and signature2 are signatures.
weights is a list of five weights.

Return the score for signature1 and signature2.
"""

# """
# signature1과 signature2는 시그니처입니다.
# weights는 다섯 가지 가중치의 리스트입니다.

# signature1과 signature2에 대한 점수를 반환합니다.
# """


# 코드 7.16

"""
signatures_dict is a dictionary mapping keys to signatures.
unknown_signature is a signature.
weights is a list of five weights.
Return the key whose signature value has the lowest 
score with unknown_signature.
"""

# """
# signatures_dict는 키를 시그니처에 매핑하는 딕셔너리입니다.
# unknown_signature는 시그니처입니다.
# weights는 다섯 가지 가중치의 리스트입니다.
# unknown_signature와 가장 낮은 점수를 가진 시그니처 값과 키를 반환합니다.
# """


# 코드 7.17

"""
mystery_filename is the filename of a mystery book whose 
author we want to know.
known_dir is the name of a directory of books.

Return the name of the signature closest to 
the signature of the text of mystery_filename.
"""

# """
# mystery_filename은 우리가 저자를 알고 싶어하는 미스터리 책의 파일 이름입니다.
# known_dir은 책의 디렉토리 이름입니다.

# mystery_filename의 텍스트의 시그니처에 가장 가까운 시그니처의 이름을 반환합니다.
# """


# 코드 7.18

"""
Ask user for a filename.
Get all known signatures from known_dir,
and print the name of the one that has the lowest score 
with the user's filename.	
"""

# """
# 사용자에게 파일 이름을 요청합니다.
# known_dir에서 모든 알려진 시그니처를 가져와서,
# 사용자의 파일 이름과 가장 낮은 점수를 가진 시그니처의 이름을 출력합니다.
# """


# 코드 8.8

"""
classroom is a list of lists
Each ' ' is an empty seat
Each 'S' is a student
Find the most students seated consecutively in a row
"""

# """
# classroom은 리스트의 리스트입니다
# 각 ' '는 빈 좌석입니다
# 각 'S'는 학생입니다
# 한 줄에 연속으로 앉아 있는 학생들 중 가장 많은 수를 찾습니다
# """


# 코드 8.9

"""
classroom is a list of lists
Each ' ' is an empty seat
Each 'S' is a student

Find the most students seated consecutively in a row
"""

# """
# classroom은 리스트의 리스트입니다
# 각 ' '는 빈 좌석입니다
# 각 'S'는 학생입니다

# 한 줄에 연속으로 앉아 있는 학생들 중 가장 많은 수를 찾습니다
# """


# 코드 9.1

"""
The clipboard contains lines of text.
Clean up the text by removing any | or space
characters from the beginning of each line.
Replace the clipboard with the cleaned text.
"""

# """
# 클립보드에는 텍스트의 줄들이 포함되어 있습니다.
# 각 줄의 시작 부분에서 | 또는 공백 문자를 제거하여 텍스트를 정리합니다.
# 클립보드를 정리된 텍스트로 교체합니다.
# """


# 코드 9.2

"""
The clipboard contains lines of text.
Clean up the text by removing any | or space
characters from the beginning of each line.
Replace the clipboard with the cleaned text.
"""

# """
# 클립보드에는 텍스트의 줄들이 포함되어 있습니다.
# 각 줄의 시작 부분에서 | 또는 공백 문자를 제거하여 텍스트를 정리합니다.
# 클립보드를 정리된 텍스트로 교체합니다.
# """


# 코드 9.3

"""
report_dir is a directory of reports named 1.pdf, 2.pdf, and so on.
These files may be one page or more than one page.

cover_dir is a directory of covers, with one cover per report.
The filenames in this directory are cover1.pdf, cover2.pdf, and so on.
Each of these files is one page.

Add the cover to the beginning of each report,
and store all resulting pdfs in final_dir.
"""

# """
# report_dir은 1.pdf, 2.pdf 등으로 명명된 보고서의 디렉토리입니다.
# 이 파일들은 한 페이지 또는 그 이상의 페이지일 수 있습니다.

# cover_dir은 보고서당 하나의 커버가 있는 커버의 디렉토리입니다.
# 이 디렉토리의 파일 이름은 cover1.pdf, cover2.pdf 등입니다.
# 이 파일들 각각은 한 페이지입니다.

# 각 보고서의 시작 부분에 커버를 추가하고,
# 모든 결과 pdf를 final_dir에 저장합니다.
# """


# 코드 9.4
"""
report_dir is a directory of reports named 1.pdf, 2.pdf, and so on.
These files may be one page or more than one page.

cover_dir is a directory of covers, with one cover per report.
The filenames in this directory are cover1.pdf, cover2.pdf, and so on.
Each of these files is one page.

Add the cover to the beginning of each report,
and store all resulting pdfs in final_dir.
"""

# """
# report_dir은 1.pdf, 2.pdf 등으로 명명된 보고서의 디렉토리입니다.
# 이 파일들은 한 페이지 또는 그 이상의 페이지일 수 있습니다.

# cover_dir은 보고서당 하나의 커버가 있는 커버의 디렉토리입니다.
# 이 디렉토리의 파일 이름은 cover1.pdf, cover2.pdf 등입니다.
# 이 파일들 각각은 한 페이지입니다.

# 각 보고서의 시작 부분에 커버를 추가하고,
# 모든 결과 pdf를 final_dir에 저장합니다.
# """


# 코드 9.5

"""
report_dir is a directory of reports named 1.pdf, 2.pdf, and so on.
These files may be one page or more than one page.

cover_dir is a directory of covers, with one cover per report.
The filenames in this directory are cover1.pdf, cover2.pdf, and so on.
Each of these files is one page.

Add the cover to the beginning of each report,
and store all resulting pdfs in final_dir.
"""

# """
# report_dir은 1.pdf, 2.pdf 등으로 명명된 보고서의 디렉토리입니다.
# 이 파일들은 한 페이지 또는 그 이상의 페이지일 수 있습니다.

# cover_dir은 보고서당 하나의 커버가 있는 커버의 디렉토리입니다.
# 이 디렉토리의 파일 이름은 cover1.pdf, cover2.pdf 등입니다.
# 이 파일들 각각은 한 페이지입니다.

# 각 보고서의 시작 부분에 커버를 추가하고,
# 모든 결과 pdf를 final_dir에 저장합니다.
# """


# 코드 9.6

"""
fname is the name of a png file.

While the file fname exists, add an _ character
right before the .png part of the filename;
e.g. 9595.png becomes 9595_.png.

Return the resulting filename.
"""

# """
# fname은 png 파일의 이름입니다.

# 파일 fname이 존재하는 동안, 파일명의 .png 부분 바로 앞에 _ 문자를 추가합니다;
# 예를 들어, 9595.png는 9595_.png가 됩니다.

# 결과적으로 생성된 파일명을 반환합니다.
# """


# 코드 9.7

"""
fname is a filename like pictures1/1262.png.
target_dir is the name of a directory.

Compare the file fname to all files in target_dir.
If fname is not identical to any file in target_dir, copy it to target_dir
"""

# """
# fname은 pictures1/1262.png와 같은 파일 이름입니다.
# target_dir은 디렉토리의 이름입니다.

# 파일 fname을 target_dir 내의 모든 파일과 비교합니다.
# 만약 fname이 target_dir 내의 어떤 파일과도 동일하지 않다면, 그것을 target_dir로 복사합니다.
# """


# 코드 9.8

"""
dirs is a list of directory names.
target_dir is the name of a directory.

Check each file in the directories and compare it to all files in target_dir.
If a file is not identical to any file in target_dir, copy it to target_dir
"""

# """
# dirs는 디렉토리 이름들의 리스트입니다.
# target_dir은 디렉토리의 이름입니다.

# 디렉토리의 각 파일을 확인하고 target_dir의 모든 파일과 비교합니다.
# 파일이 target_dir의 어떤 파일과도 동일하지 않다면, 그것을 target_dir로 복사합니다.
# """


# 코드 10.1

"""
length is an integer.

Return a string of the given length, where each character
is a digit from 0 to 9, and with no repeated digits.
"""

# """
# length는 정수입니다.

# 각 문자가 0에서 9까지의 숫자이며,
# 반복되는 숫자가 없는 주어진 길이의 문자열을 반환합니다.
# """


# 코드 10.2

"""
length is an integer.

Return a string of the given length, where each character
is a digit from 0 to 9, and with no repeated digits.
"""

# """
# length는 정수입니다.

# 각 문자가 0에서 9까지의 숫자이며,
# 반복되는 숫자가 없는 주어진 길이의 문자열을 반환합니다.
# """


# 코드 10.3

"""
length is an integer.

Keep asking the player to enter a string where each character 
is a digit from 0 to 9, until they enter a valid guess.
A valid guess has the given length and has no repeated digits.
"""

# """
# length는 정수입니다.

# 플레이어가 유효한 추측을 입력할 때까지
# 각 문자가 0에서 9까지의 숫자인 문자열을 입력하도록 계속 요청합니다.
# 유효한 추측은 주어진 길이를 가지며 반복되는 숫자가 없습니다.
# """


# 코드 10.4

"""
guess and secret_code are strings of the same length.

Return a list of two values:
the first value is the number of indices in guess where
the character at that index matches the character at the
same index in secret_code; the second value is the 
number of indices in guess where the character at that 
index exists at a different index in secret_code.
"""

# """
# guess와 secret_code는 같은 길이의 문자열입니다.

# 두 가지 값을 포함하는 리스트를 반환합니다:
# 첫 번째 값은 guess의 인덱스 중에서
# 해당 인덱스의 문자가 secret_code의 같은 인덱스의 문자와 일치하는 인덱스의 수입니다;
# 두 번째 값은 guess의 인덱스 중에서
# 해당 인덱스의 문자가 secret_code의 다른 인덱스에 존재하는 인덱스의 수입니다.
# """


# 코드 10.5

"""
Generate a random string with num_digits digits.
The player has num_guesses guesses to guess the random 
string. After each guess, the player is told how many 
digits in the guess are in the correct place, and how 
many digits exist but are in the wrong place.
"""

# """
# num_digits 자릿수를 가진 무작위 문자열을 생성합니다.
# 플레이어는 무작위 문자열을 추측하기 위해 num_guesses번의 추측 기회를 가집니다.
# 각 추측 후에, 플레이어에게 추측에서 몇 개의 숫자가 올바른 위치에 있는지, 그
# 리고 몇 개의 숫자가 존재하지만 잘못된 위치에 있는지 알려줍니다.
# """


# 코드 10.7

"""
player1 is the number of chips that player 1 has.
player2 is the number of chips that player 2 has.
rolls is the last list of dice rolls.

Return True if the game is over, False otherwise.

The game is over if player1 has at least 30 points, 
or player 2 has at least 30 points, 
or there are 5 rolls none of which is a 1.
"""

# """
# player1은 플레이어 1이 가진 칩의 수입니다.
# player2는 플레이어 2가 가진 칩의 수입니다.
# rolls는 주사위 굴림의 마지막 리스트입니다.

# 게임이 끝났다면 True를 반환하고, 그렇지 않다면 False를 반환합니다.

# player1이 적어도 30점을 가지고 있거나,
# player 2가 적어도 30점을 가지고 있거나,
# 1이 없는 5번의 굴림이 있으면 게임이 끝납니다.
# """


# 코드 10.8

"""
Create a list of n random integers between 1 and 6.
Print each of these integers, and return the list.
"""

# """
# 1과 6 사이의 n개의 무작위 정수로 구성된 리스트를 생성합니다.
# 이 정수들 각각을 출력하고, 리스트를 반환합니다.
# """


# 코드 10.9

"""
Return True if the turn is over, False otherwise.

The turn is over if any of the rolls is a 1, 
or if there are exactly five rolls.
"""

# """
# 턴이 끝났다면 True를 반환하고, 그렇지 않다면 False를 반환합니다.

# 어떤 굴림이 1이거나, 정확히 다섯 번 굴렸다면 턴이 끝납니다.
# """


# 코드 10.10

"""
The pot has pot_chips chips.

Take a full turn for the current player and, once done, 
return a list of two values:
the number of chips in the pot, and the final list of dice rolls.

Begin by rolling 1 die, and put 1 chip into the pot.
Then, if the turn isn't over, ask the player whether 
they'd like to continue their turn.
If they respond 'n', then the turn is over.
    If they respond 'y', then roll one more die than last time, 
    and add 1 chip to the pot for each die that is rolled.
    (for example, if 3 dice were rolled last time, then 
    roll 4 dice and add 4 chips to the pot.)
If the turn is not over, repeat by asking the player again 
whether they'd like to continue their turn.
"""

# """
# 팟에는 pot_chips 칩이 있습니다.

# 현재 플레이어에 대해 전체 턴을 진행하고, 완료되면,
# 두 가지 값을 포함하는 리스트를 반환합니다:
#  팟에 있는 칩의 수, 그리고 주사위 굴림의 최종 리스트.

# 1개의 주사위를 굴리고 팟에 1개의 칩을 넣습니다.
# 그런 다음, 턴이 끝나지 않았다면 플레이어에게 턴을 계속 진행하고 싶은지 물어봅니다.
# 그들이 'n'으로 응답하면 턴이 끝납니다.
#     그들이 'y'로 응답하면, 마지막 번보다 하나 더 많은 주사위를 굴리고,
#     굴린 주사위마다 팟에 1개의 칩을 추가합니다.
#     (예를 들어, 마지막에 3개의 주사위를 굴렸다면,
#     4개의 주사위를 굴리고 팟에 4개의 칩을 추가합니다.)
# 턴이 끝나지 않았다면, 플레이어에게 다시 물어보는 것을 반복하여
# 턴을 계속 진행하고 싶은지 물어봅니다.
# """


# 코드 10.11

"""
Return True if the player wins chips, False otherwise.

The player wins the chips if none of the rolls is a 1.
"""

# """
# 플레이어가 칩을 이기면 True를 반환하고, 그렇지 않으면 False를 반환합니다.

# 굴림 중 1이 없으면 플레이어가 칩을 이깁니다.
# """


# 코드 10.12

"""
player1 is the number of chips that player 1 has.
player2 is the number of chips that player 2 has.
rolls is the last list of dice rolls.
current_player is the current player (1 or 2).

If the game is not over, switch current_player to the other player.
Return the new current_player.
"""

# """
# player1은 플레이어 1이 가진 칩의 수입니다.
# player2는 플레이어 2가 가진 칩의 수입니다.
# rolls는 주사위 굴림의 마지막 리스트입니다.
# current_player는 현재 플레이어입니다 (1 또는 2).

# 게임이 끝나지 않았다면, current_player를 다른 플레이어로 바꿉니다.
# 새로운 current_player를 반환합니다.
# """


# 코드 10.13

"""
Play the game until the game is over.

The pot starts with 0 chips, and each player starts with 0 chips.

Randomly decide whether player 1 or player 2 goes first.
Before each turn, print three lines of information:
1. The number of chips in the pot
2. The number of chips that each player has
3. Whether it is player 1's turn or player 2's turn

Take a full turn for the current player. 
If they won the chips, add the chips in the pot to the 
total for that player and reset the pot to have 0 chips.

Then, switch to the other player's turn.

Once the game is over, print the current player 
(that’s the player who won).
"""

# """
# 게임이 끝날 때까지 게임을 진행합니다.

# 팟은 0개의 칩으로 시작하고, 각 플레이어는 0개의 칩으로 시작합니다.

# 플레이어 1 또는 플레이어 2 중 누가 먼저 할지 무작위로 결정합니다.
# 각 턴 전에, 세 줄의 정보를 출력합니다:
# 1. 팟에 있는 칩의 수
# 2. 각 플레이어가 가진 칩의 수
# 3. 플레이어 1의 턴인지 플레이어 2의 턴인지

# 현재 플레이어에 대해 전체 턴을 진행합니다.
# 만약 그들이 칩을 이겼다면,
# 팟의 칩을 그 플레이어의 총계에 더하고 팟을 0개의 칩을 가지도록 재설정합니다.

# 그런 다음, 다른 플레이어의 턴으로 전환합니다.

# 게임이 끝나면, 현재 플레이어를 출력합니다 (즉, 이긴 플레이어를 출력합니다).
# """


# 코드 10.14

"""
Play the game until the game is over.

The pot starts with 0 chips, and each player starts with 0 chips.

Randomly decide whether player 1 or player 2 goes first.

Before each turn, print three lines of information:
1. The number of chips in the pot
2. The number of chips that each player has
3. Whether it is player 1's turn or player 2's turn
Take a full turn for the current player. 
If they won the chips, add the chips in the pot to the
total for that player
and reset the pot to have 0 chips.

Then, switch to the other player's turn.

Once the game is over, print the current player 
(that's the player who won).
"""

# """
# 게임이 끝날 때까지 게임을 진행합니다.

# 팟은 0개의 칩으로 시작하고, 각 플레이어는 0개의 칩으로 시작합니다.

# 플레이어 1 또는 플레이어 2 중 누가 먼저 할지 무작위로 결정합니다.

# 각 턴 전에, 세 줄의 정보를 출력합니다:
# 1. 팟에 있는 칩의 수
# 2. 각 플레이어가 가진 칩의 수
# 3. 플레이어 1의 턴인지 플레이어 2의 턴인지 현재 플레이어에 대해 전체 턴을 진행합니다.

# 만약 그들이 칩을 이겼다면, 팟의 칩을 그 플레이어의 총계에 더하고
# 팟을 0개의 칩을 가지도록 재설정합니다.

# 그런 다음, 다른 플레이어의 턴으로 전환합니다.

# 게임이 끝나면, 현재 플레이어를 출력합니다
# (즉, 이긴 플레이어를 출력합니다).
# """


# 코드 11.1

"""	
Keep asking the user to enter a password until they provide 
a strong password.
A strong password has at least one uppercase character, one 
number, and one punctuation character.
"""

# """
# 사용자에게 강력한 비밀번호를 제공할 때까지 비밀번호를 입력하라고 계속 요청합니다.
# 강력한 비밀번호는 하나 이상의 대문자, 하나 이상의 숫자, 하나 이상의 구두점 문자를 포함합니다.
# """
